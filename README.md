# @absolutejs/rag

Standalone RAG runtime extracted from AbsoluteJS.

This package currently contains retrieval, ingestion, sync, connector, and evaluation code.
A later cleanup pass should extract the remaining shared AI substrate into @absolutejs/ai.
