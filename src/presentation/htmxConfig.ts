import type {
  RAGHTMXConfig,
  RAGHTMXWorkflowRenderConfig,
} from "../../types/engine";

export const createRAGHTMXConfig = (config: RAGHTMXConfig) => config;

export const createRAGHTMXWorkflowRenderConfig = (
  config: RAGHTMXWorkflowRenderConfig,
) => config;
